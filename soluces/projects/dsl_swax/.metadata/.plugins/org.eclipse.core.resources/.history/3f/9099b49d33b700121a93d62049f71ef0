package simulation.api

import simulation.Duration._

object SimulationApiTest extends App {
  val dayDuration = 10000 years
  val population = Population(10000000, dayDuration)
  population.simulate()
}