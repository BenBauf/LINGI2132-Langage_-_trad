package simulation.api

object SimulationApiTest extends App {
  val dayDuration = 365 * 10
  val population = new Population(dayDuration)
  population.init(10000)
  model.simulate(dayDuration, population)
}
